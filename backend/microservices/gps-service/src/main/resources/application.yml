server:
  port: 8088
  servlet:
    context-path: /api/v1/gps

spring:
  application:
    name: gps-service
  datasource:
    url: jdbc:postgresql://localhost:5432/worksphere_gps
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
